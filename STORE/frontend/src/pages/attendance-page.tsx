import { Card } from '@/components/ui/card';

export function AttendancePage(): JSX.Element {
  return <Card><h3 className="text-lg font-bold tracking-tight">Attendance</h3><p className="mt-2 text-sm text-zinc-500">Clock-in and clock-out UI will render here.</p></Card>;
}
